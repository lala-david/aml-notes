# 개발에서는 아무것도 설정하지 않아도 된다.
# 프론트는 같은 출처로 /api 를 부르고, Vite 가 아래 주소로 넘긴다.

# 백엔드를 8000 이 아닌 곳에 띄웠을 때만 바꾼다.
# VITE_API_TARGET=http://127.0.0.1:8000

# 프론트와 백엔드를 서로 다른 호스트에 배포할 때만 쓴다.
# 값을 넣으면 프록시를 거치지 않고 브라우저가 직접 부르므로
# 백엔드 CORS 에 프론트 출처를 허용해야 한다.
# VITE_API_BASE=https://api.example.com
